# Copy this file to frontend/.env.docker and adjust if needed.

# Used by browser-side API calls from frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Used by server-side rendering inside the frontend container
API_BASE_URL_SERVER=http://backend:8000
